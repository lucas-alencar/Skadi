export interface Card {
    id: string;
    name: string;
    desc: string;
    idList: string;
}

export interface TrelloResponse{
    id: string;
    name: string;
    desc: string;
    url: string;
}